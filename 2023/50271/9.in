void getSum(int a, int b) {
    int sum = a + b;
}
char toUppercase(char c) {
    if (c >= 97 && c <= 122) {
        return c-(97 -65);
    } else {
        return c;
    }
}

int main() {
    getSum(5,7);

    char lowercaseChar = 74;
    char uppercaseChar = toUppercase(lowercaseChar);

    return 0;
}
